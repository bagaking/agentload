<!--
meta:
  目的: M01 Sprint 1——把进行中的加固批次落成带 tag 的、有性能基线的发布门，并宣布功能合并冻结；后续所有 CI 性能预算（M02_S02）以本 sprint 记录的基线为参照。
  日期: 2026-07-26
  来源: judge 里程碑规划 M01_S01；仓库当前工作区状态（大量已修改 Go/UI 文件、新增 tray_test.go 与 docs/README.md、重建的 ui/dist chunks）；CURRENT.md §1.2 质量门。
-->

# M01_S01 · 加固发布门与基线 tag

## OKR

- **Objective**：main 上进行中的加固批次以可验证、可回溯的方式收口，形成后续一切工作的参照基线。
- KR1：四道质量门（`go test ./...`、`npm --prefix ui run build`、`node scripts/validate_locales.js`、`./build_macos_app.sh`）在 tag commit 上全绿并留档。
- KR2：4 项基线指标（空闲 CPU %、popover 冷/热打开耗时、/api/snapshot p95、二进制体积）以注明测量方法的形式写入仓库文档。
- KR3：基线 tag 发布 + 功能合并冻结生效，直至 M01_S02 验收退出。

## Checklist

- [x] 落地并发正确性 / 死代码清理 / 性能 / 文档的加固批次（**已在当前工作区落地**：observer/process/history/server/metrics 等 Go 文件与 UI 语义层修改、新增 tray_test.go、docs/README.md、重建 dist chunks；待整理提交）
- [x] 把脏工作区整理为语义清晰的干净 commit（已完成：`1c66208` fix(core) 后端加固、`817b27c` fix(ui) 前端修复与性能、`70fc29f` docs 架构/API 文档、`dfe5a4b` fix(core) 审查修复——部分快照不落缓存）
- [x] 运行并记录四道质量门结果（2026-07-26 于 `dfe5a4b`：`go vet`/`go test ./...` 绿、`node scripts/validate_locales.js` 绿（432 键 ×3 locale，tsc + vite 通过）、`./build_macos_app.sh` 绿，版本 2026.07.26.214004 已安装验证）
- [x] 校验嵌入的 ui/dist 资产与当前 ui/src 构建一致（`817b27c` 中 dist 与 src 同一构建产出，旧 chunk 已删）
- [ ] 在仓库内文档记录基线指标：空闲 CPU %、popover 冷/热打开 ms、/api/snapshot p95 ms、二进制 MB——每项写明测量方法与硬件环境
- [ ] 打基线 tag 并在 CURRENT.md 声明功能合并冻结（至 M01_S02 退出）

## 审查遗留项（对抗审查 advisory，非阻塞；本 sprint 内处理或明确移交）

三路对抗审查（Go 并发 / 语义 / 前端）共 19 条：1 条高危已修（`dfe5a4b`），以下为值得跟进的 advisory（其余为低价值风格项，不录）：

- [ ] `server.go` Origin 校验可被 DNS rebinding 绕过——应比对实际绑定地址而非 `r.Host`
- [x] `transcripts.go` 健康 waiter 不再消费被取消 owner 的部分结果；不完整 flight 会由健康 waiter 接管重试
- [x] `history.go` 压缩文件在 rename 前 `Sync()`，并由 `internal/historyfile` 跨进程锁串行化 load/compact/append
- [x] `system_resources.go` stop/start 已增加 generation 保护，旧 goroutine 的迟到结果会被拒绝
- [x] `tray.go` 已删除仅供测试调用的 `mergeRuntimeTrendsLocked`，测试与生产统一走 `rememberSnapshot`
- [ ] `ui/src/main.tsx` 键盘聚焦行时 hover 检查器仍需浏览器回归确认定位行为
- [x] `ui/src/snapshot/useSnapshotController.ts` 在 `/api/refresh` 失败后直接结束本轮，不再空跑 5 步轮询梯
- [x] `ui/src/system/useLiveSystemResources.ts` 的 active 已同时受 popover 可见性约束，隐藏后停止 2s 轮询
- [x] `ui/src/types/app.ts` 已删除死类型 `LogTab`、`SelectedView`
- [ ] `ui/src/styles/system-resource-inspector.css` 新 token 落在组件 CSS，违反 styles.css 单一源规则——移交 M01_S03
- [ ] `ui/src/i18n.ts` `attentionAgentsTooltip` 文案与后端 needs_review 规则（含 stale）不同步

## 验收标准（量化）

1. 四道质量门在 tag commit 上全部绿。
2. 基线性能数字已入仓库文档，且每项注明测量方法（空闲 CPU %、popover 打开 ms、snapshot p95 ms、二进制 MB）。
3. 针对该 tag 的已知正确性回归为 0。
4. 合并冻结已在 CURRENT.md 记录并实际生效。

## 依赖与引用

- PLAN.md §5 技术基座与硬约束、§6 Roadmap 总览（M01 定位：先加固后功能，见 OPINIONS_001 D-002）
- CURRENT.md §1.2 项目质量门（四道门定义的 SSOT，此处不复述）
- docs/agent-load-parity-checklist.md（状态声明须附仓库内证据引用的约定）
- DOCREF_003 §3 第 4 条（性能预算为何必须有基线：上"Significant Energy"榜即卸载）
