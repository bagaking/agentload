<!--
meta:
  目的: M02 Sprint 1——从 adapter 能力声明生成 vendor × 信号族 的证据覆盖矩阵（Evidence Coverage Matrix），一份源同时驱动 API、UI 面板与文档；此后它是所有 vendor 发布的诚实分级门。
  日期: 2026-07-26
  来源: judge 里程碑规划 M02_S01；docs/neutral-observation-principles.md（missing stays missing）；docs/agent-load-metric-semantics.md。
-->

# M02_S01 · 证据覆盖矩阵：UI / API / 文档三端同源

## OKR

- **Objective**：把"诚实分级"从文档原则变成可执行的机器事实——任何能力变化在三端零手工同步。
- KR1：改一处 adapter 能力声明，API、UI 面板、文档页三端同步更新（测试证明零手工编辑）。
- KR2：100% 渲染的 snapshot 数据可追溯到某个矩阵 cell 及其命名的证据来源。
- KR3：审计测试证明未观测 cell 渲染为带标注的空缺——绝无零值、估算或推断的假对等。

## Checklist

- [ ] 从 M01_S02 的 adapter 能力声明生成 vendor × 信号族矩阵（信号族：presence/resources、sessions/roles、recent movement、tokens/cost、quota windows、workspace context、consented telemetry）—— **部分：已生成「4 能力槽 × 10 vendor」，7 信号族仍无代码结构，见 §「已落地部分」**
- [ ] 定义 cell 状态：observed / partial / unavailable / not_configured，每态命名其证据来源——延伸既有诚实能力模式（thermal 的 "unavailable"、telemetry 的 "not_configured"）
- [ ] 矩阵以 /api endpoint 提供，dashboard 面板从同一生成源渲染（SSOT）—— **未做；`capabilityMatrix()` 目前只有测试一个调用方**
- [x] **文档页从同一源生成，使 docs 与 code 无法漂移**（2026-09-20，见 §「已落地部分」）
- [ ] 所有 cell 标签经 metric_semantics.go 与 ui/src/lib/metricSemantics.ts 路由
- [ ] 每个 cell 状态与信号族补齐 en/zh/ja 字符串
- [ ] 把矩阵确立为发布门：vendor 以其 cells 诚实支撑的层级发布，不多不少 —— **部分：`TestCapabilityMatrixDocMatchesTheRegistry` 已是 code→doc 的门，尚未成为 vendor 发布分级的门**

## 已落地部分（2026-09-20）

**触发事件不是排期，是一次真实漂移**：Antigravity 作为第二证据根接进 gemini adapter（`M02_S05.006.RSI`）后，`docs/coding-agent-evidence-adapters.md` 的手写能力表**完全没有反应**——全仓库没有任何东西比对过代码与文档。这正是本 sprint 存在的理由，所以先把这一格补上。

落地范围（**仅 code→doc 一端，API/UI 两端仍未做**）：

- `agent_registry.go`：`codingAgentAdapter` 增 `Evidence []string`（该 adapter 实际读取的磁盘形状，每个独立布局一条）与 `Note string`（为什么某槽缺席／某槽带什么陷阱）。十个 adapter 全部声明完毕。
- `capability_matrix.go`（新）：`capabilityMatrix()` 直接读注册表的四个能力槽——**nil 槽渲染为 `unsupported`，绝不软化、绝不从兄弟槽推断**；`renderCapabilityMatrixMarkdown` 产出带标记的生成区；`spliceCapabilityMatrix` 只替换标记之间的内容，标记缺失时报错而非猜测（防止手写散文被整体覆盖）。
- 文档表格改为 `<!-- BEGIN/END GENERATED CAPABILITY MATRIX -->` 包裹的生成区，`UPDATE_CAPABILITY_MATRIX=1 go test . -run TestCapabilityMatrixDocMatchesTheRegistry` 重新生成。
- 三个测试：doc 与注册表一致性门；nil 槽必须渲染 unsupported；**凡是解析 transcript 的 adapter 必须声明证据形状**，且 gemini 必须声明两个根（这条专门盯住「往既有 adapter 加第二个证据根」这个已经发生过的失败形状）。

**新表比旧手写表多出的真实信息**：`Evidence discovery` 列（旧表没有）、每个 adapter 实际读取的磁盘路径形状、以及 **antigravity 根**——旧表里它不存在。

**变异测试确认门有效**（两类都试过）：

| 注入 | 结果 |
|---|---|
| 删掉 gemini 的 antigravity 证据声明 | 两个测试同时变红（doc 陈旧 + gemini 应声明两个根） |
| 给 hermes 塞一个它并不具备的 Usage 槽 | doc 陈旧，实测打印出 `\| hermes \| … \| supported \|` 的错误行 |

**明确未做，不要误读为已完成**：

- 7 个信号族（presence/resources、sessions/roles、recent movement、tokens/cost、quota windows、workspace context、consented telemetry）**没有对应的代码结构**。代码里只有 4 个能力槽。生成的是「4 槽 × 10 vendor」，不是「7 族 × N vendor」。
- 4 态 cell（observed/partial/unavailable/not_configured）**未实现**，当前只有二元 supported/unsupported。因为能力槽本身就是二元的（nil 或非 nil），造出 partial 会是虚构。
- `/api` endpoint 与 dashboard 面板**未做**。
- 因此 KR1「三端同步」目前只满足「一端（docs）+ 代码源」，KR2/KR3 未触及。

**这笔账的性质**：4 槽 → 7 族是需要先定义信号族与其证据映射的**设计工作**，不是接线工作。在那之前生成一张 7 列的表只会产出一堆无依据的 cell——违反本 sprint 自己的 KR3。

## 验收标准（量化）

1. 修改一处 adapter 能力声明 → API、UI 面板、文档零手工编辑同步更新（测试证明）。
2. 100% 已渲染 snapshot 数据可追溯到矩阵 cell 与命名证据来源。
3. 审计测试：未观测 cell 渲染为标注空缺，绝非零值/估算/推断对等。
4. locale 校验器绿，全部新 key 三语齐备。

## 依赖与引用

- M01_S02（能力声明是本矩阵的唯一数据源）
- PLAN.md §0 五大支柱（"truthful"不变式先行——见 OPINIONS_001 D-004）
- docs/neutral-observation-principles.md（cell 状态语义的原则出处）
- docs/agent-load-metric-semantics.md + ui/src/lib/metricSemantics.ts（标签路由的强制通道）
- DOCREF_001 洞察 2（诚实证据语义是可防御的信任地位）；DOCREF_004 第 2 条（"misleading monitor"骂名的成因即本矩阵要杜绝的行为）
- 后续消费方：M02_S03/S04（新 vendor 点亮 cell）、M04（cost/quota cell）、M05_S04（consented 层级）、M06_S04（两档构建的 cell diff）
