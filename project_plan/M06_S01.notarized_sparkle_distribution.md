<!--
meta:
  目的: M06 Sprint 1——信任级分发：Developer ID 签名 + notarization、Sparkle 2 自动更新（EdDSA 签名 delta + appcast）、单 target 双 scheme 混合（未来 MAS 构建直接省略 Sparkle）、Homebrew cask、发布清单成文——当前 v1 用户没有任何更新路径，此 sprint 补上分发的全部信任信号。
  日期: 2026-07-26
  来源: judge 里程碑规划 M06_S01；DOCREF_003 §2（Sparkle vs MAS 权衡：v1 就要带自动更新；MAS 构建捆 Sparkle 会被拒；notarization 是店外分发硬要求）；DOCREF_001 §2（CCSeva 因 ad-hoc 签名被 Gatekeeper 拦——反面教材）。
-->

# M06_S01 · 信任级分发：notarize、Sparkle、Homebrew cask

## OKR

- **Objective**：让"正常人"在干净机器上一次装上、之后自动升级——签名/公证/更新/cask 四件套齐备，不留 CCSeva 式 Gatekeeper 尴尬。
- KR1：公证构建在干净机器过 Gatekeeper 零警告（anti-CCSeva 门）。
- KR2：vN → vN+1 端到端自动更新验证通过，含签名 delta。
- KR3：MAS scheme 构建零 Sparkle 符号（审计）。

## Checklist

- [ ] build_macos_app.sh 加入 Developer ID 签名与 notarization
- [ ] 集成 Sparkle 2 自动更新：EdDSA 签名 delta 更新 + appcast（当前无任何更新路径——v1 用户否则永远停在 v1）
- [ ] 采用单 target 双 scheme 混合：未来 MAS 构建直接省略 Sparkle（MAS 捆 Sparkle 即被拒，成文坑）
- [ ] 发布 Homebrew cask
- [ ] 发布清单成文：四大构建门 + perf gate + 按 macOS 版本 popover QA（收编 M05_S03 清单）

## 验收标准（量化）

1. 公证构建在干净机器过 Gatekeeper，零警告（anti-CCSeva 门——DOCREF_001 §2 记录其 ad-hoc 签名被拦始末）。
2. vN → vN+1 端到端自动更新验证，delta 签名有效。
3. `brew install` 从 cask 成功。
4. MAS scheme 构建不含 Sparkle 符号（审计）。

## 依赖与引用

- M05_S03（按版本 QA 清单入发布清单）；M01_S01（四门 + 基线）；M02_S02（perf gate）
- PLAN.md §6 Roadmap（M06 = 触达与耐久）；§0（impactful 支柱：价值归于成为 ambient default 者——分发是前提）
- docs/apple-distribution-readiness.md（现状记录：ps/lsof 无 sandbox 证明——本 sprint 走直发通道，不等 MAS 决策）
- build_macos_app.sh 与 macos/（既有构建脚本与草案 entitlements 是改造对象）
- DOCREF_003 §2（Sparkle 双分发成文实践与链接）；DOCREF_001 洞察 5（签名/公证是品类信任信号）
- 后续消费方：M06_S04（MAS go/no-go 只需换 scheme，不动直发线）
