<!--
meta:
  目的: M01 Sprint 3——把 ~3,000 行的 ui/src/main.tsx 拆为懒加载模块，建立 popover 关键路径（fast path），为 M05_S03 的 WKWebView 预热铺路。
  日期: 2026-07-26
  来源: judge 里程碑规划 M01_S03；DOCREF_003 §2（WKWebView-in-NSPopover 首绘/白闪坑位）。
-->

# M01_S03 · main.tsx 模块拆分与 popover 快速路径

## OKR

- **Objective**：popover 关键路径的 JS 载荷显著瘦身，首绘接近原生 glance 体验，且刷新纪律零回归。
- KR1：popover 关键路径 JS 载荷较 M01_S01 基线减少 ≥30%。
- KR2：基线硬件上热 popover 首绘 ≤150ms（或记录差距 + 后续方案）。
- KR3：隐藏 surface 零 snapshot 请求，有回归测试锁定。

## Checklist

- [ ] main.tsx 拆为模块级 chunk：dashboard shell、trend suite、diagnostics、system deck，全部懒加载
- [ ] popover 关键路径完全排除 trend/diagnostics bundle
- [ ] 拆分后保留 visibility-aware 轮询、refresh_slot_id 合并、ETag/If-None-Match 缓存，并配回归测试
- [ ] bundle 结构面向 WKWebView 预热设计（M05_S03 消费）
- [ ] 在新 chunk 布局上重建嵌入的 ui/dist，并复跑 locale 校验器
- [ ] 对照 M01_S01 基线重测 popover 首绘

## 验收标准（量化）

1. popover 关键路径 JS 载荷较基线 ≥30% 缩减。
2. 热 popover 首绘 ≤150ms（基线硬件），或在仓库文档记录差距与跟进计划。
3. 隐藏 surface 零 snapshot 请求（回归测试证明）。
4. 四道质量门全绿，含新布局上的 locale parity。

## 依赖与引用

- M01_S01（首绘与载荷基线数字的出处）
- PLAN.md §5（刷新纪律硬约束：30s 下限、slot 合并、刷新绝非导航/重置事件）
- docs/agent-load-ui-design-system.md（拆分不得改变指标含义与视觉语义）
- DOCREF_003 §2 WKWebView 坑位与 §3 第 3 条 popover 礼仪（首绘延迟/白闪正是用户感知的"非原生感"来源）
- 后续消费方：M05_S03 instant popover 预热
